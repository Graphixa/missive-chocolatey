name: Test

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  smoke-test:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Full smoke test (pack, install, verify shortcuts, uninstall)
        shell: pwsh
        run: ./scripts/Test-Package.ps1
